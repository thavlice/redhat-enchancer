apiVersion: v1
kind: Service
metadata:
  name: {{ include "redhat-enhancer-chart.fullname" . }}
  labels:
    {{- include "redhat-enhancer-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redhat-enhancer-chart.selectorLabels" . | nindent 4 }}
