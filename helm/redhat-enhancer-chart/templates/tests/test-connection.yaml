apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redhat-enhancer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "redhat-enhancer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redhat-enhancer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
